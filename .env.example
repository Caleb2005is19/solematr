# This file is an example. Create a .env.local file and add your credentials.
# DO NOT COMMIT your .env.local file to Git.

# -------------------------------------
# --- CLIENT-SIDE FIREBASE CONFIG ---
# -------------------------------------
# These are used in the browser and are safe to be public.
# Find these in your Firebase project settings under:
# Project settings > General > Your apps > Web app > Config
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=

# -------------------------------------
# --- SERVER-SIDE ADMIN CONFIG ---
# -------------------------------------
# This is used on the server and MUST BE KEPT SECRET.
# 1. Go to Firebase Project settings > Service accounts > Generate new private key.
# 2. A JSON file will be downloaded.
# 3. Base64-encode the ENTIRE content of that JSON file.
# 4. Paste the resulting long string here.
#
# You can use an online tool like base64encode.org or a command-line utility:
# `base64 -i /path/to/your/service-account-file.json`
FIREBASE_ADMIN_SERVICE_ACCOUNT_BASE64=
