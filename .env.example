# -----------------------------------------------------------------------------
# FIREBASE - CLIENT-SIDE CONFIG
#
# These are public keys for the Firebase SDK running in the browser.
# Find these in your Firebase Project Settings > General > Your Apps > Web App.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=


# -----------------------------------------------------------------------------
# FIREBASE - SERVER-SIDE ADMIN CONFIG (SECRET)
#
# This is a secret service account key for the Firebase Admin SDK.
# 1. In Firebase Console, go to Project Settings > Service Accounts.
# 2. Click "Generate new private key" to download a JSON file.
# 3. Base64-encode the ENTIRE contents of that JSON file into a single string.
#    - macOS/Linux: base64 -i /path/to/your/key.json
#    - Online Tool: base64encode.org (use "Encode files")
# 4. Paste the single long string here.
# -----------------------------------------------------------------------------
FIREBASE_ADMIN_SERVICE_ACCOUNT_BASE64=


# -----------------------------------------------------------------------------
# CLOUDINARY - IMAGE HOSTING
#
# These are used by the server action to upload images to your Cloudinary account.
# Find these in your Cloudinary Dashboard.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
