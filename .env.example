# -----------------------------------------------------------------------------
# CLIENT-SIDE FIREBASE CONFIGURATION
# -----------------------------------------------------------------------------
# These variables are exposed to the client-side and are safe to be public.
# You can find these values in your Firebase project settings under "General".
# Go to: Project Overview -> Project settings -> General -> Your apps -> Firebase SDK snippet -> Config

NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-auth-domain"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"


# -----------------------------------------------------------------------------
# SERVER-SIDE FIREBASE ADMIN CONFIGURATION
# -----------------------------------------------------------------------------
# This variable is ONLY used on the server and MUST be kept secret.
# It contains the credentials for the Firebase Admin SDK to manage your backend.

# 1. Go to your Firebase project settings:
#    Project Overview -> Project settings -> Service accounts
# 2. Click "Generate new private key". A JSON file will be downloaded.
# 3. Open the downloaded JSON file and copy its ENTIRE content.
# 4. Encode the copied content into a single-line Base64 string.
#    You can use an online tool or the following command in your terminal:
#    cat /path/to/your/service-account-file.json | base64 | tr -d '\n'
# 5. Paste the resulting Base64 string here.

FIREBASE_ADMIN_SERVICE_ACCOUNT_BASE64="your-service-account-base64-string"
