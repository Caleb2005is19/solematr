# -----------------------------------------------------------------------------
# CLIENT-SIDE FIREBASE CONFIGURATION (SAFE TO EXPOSE)
# These are exposed to the browser and are required for the app to function.
# Find these in your Firebase Project Settings > General > Your Apps > Web App.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=


# -----------------------------------------------------------------------------
# SERVER-SIDE FIREBASE ADMIN CREDENTIALS (MUST BE KEPT SECRET)
# These are used for server-side rendering and administrative tasks.
# DO NOT COMMIT THE .env.local FILE WITH THESE VALUES FILLED IN.
#
# HOW TO GET THIS VALUE:
# 1. Go to Firebase Console > Project Settings > Service accounts.
# 2. Click "Generate new private key". A JSON file will download.
# 3. Base64-encode the contents of that entire JSON file.
#    - On macOS/Linux, you can use the command:
#      base64 -w 0 /path/to/your/service-account-file.json
#    - You can also use an online base64 encoder.
# 4. Paste the resulting single long string as the value for this variable.
# -----------------------------------------------------------------------------
FIREBASE_ADMIN_SERVICE_ACCOUNT_BASE64=
